## 1. Update UI Label

- [x] 1.1 In `src/components/SourcesPanel.tsx` line 59, replace the JSX text node `Config Sources` with `Sources`

## 2. Verify

- [x] 2.1 Confirm no remaining occurrences of "Config Sources" exist in the codebase
- [x] 2.2 Visually verify the panel heading reads "Sources" in the running app
